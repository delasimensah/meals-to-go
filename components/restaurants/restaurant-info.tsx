import { FC } from "react";
import { Text } from "react-native";

type RestaurantInfoProps = {
  text?: string;
};

const RestaurantInfo: FC<RestaurantInfoProps> = () => {
  return <Text>RestaurantInfo</Text>;
};

export default RestaurantInfo;
